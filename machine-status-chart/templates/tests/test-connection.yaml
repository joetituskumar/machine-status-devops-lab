apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "machine-status-chart.fullname" . }}-test-connection"
  labels:
    {{- include "machine-status-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "machine-status-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
