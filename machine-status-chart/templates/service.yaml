apiVersion: v1
kind: Service
metadata:
  name: {{ include "machine-status-chart.fullname" . }}
  labels:
    {{- include "machine-status-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "machine-status-chart.selectorLabels" . | nindent 4 }}
